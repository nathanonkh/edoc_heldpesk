-- ============================================
-- เพิ่มเติมจาก install_additions.sql / _2.sql / _3.sql
-- เพิ่มคอลัมน์เก็บสถานะว่าเรื่องนี้เคยถูกส่งต่อไปยังส่วนกลางหรือไม่
-- เพื่อให้แสดงผลสถานะเป็น "กำลังดำเนินการ/ส่วนกลาง" หรือ "สำเร็จ/ส่วนกลาง"
-- ให้จังหวัดทราบว่าเรื่องนี้ส่วนกลางเป็นผู้ดำเนินการอยู่
-- MySQL 5.0.45 syntax เท่านั้น
-- ============================================

USE edoc_cooperative;

ALTER TABLE `issues`
  ADD COLUMN `handled_by_central` tinyint(1) default '0' COMMENT 'เคยถูกส่งต่อไปยังส่วนกลาง (แสดงผลสถานะ .../ส่วนกลาง)' AFTER `status`;
