-- =====================================================================
-- migration.sql
-- รันไฟล์นี้ 1 ครั้งก่อน deploy โค้ดที่แก้ไขแล้ว (ผ่าน phpMyAdmin ของ AppServ
-- หรือคำสั่ง mysql -u root -p edoc_cooperative < migration.sql)
--
-- เหตุผล: ระบบเปลี่ยนจาก sha1($password) (40 ตัวอักษร) มาเป็น
-- PasswordHash::hash() ซึ่งเก็บในรูปแบบ "<salt 32 ตัว>$<hmac-sha256 hex 64 ตัว>"
-- รวมความยาวประมาณ 97 ตัวอักษร จึงต้องขยายคอลัมน์ password ให้รองรับ
--
-- ปลอดภัย 100% กับข้อมูลเดิม: รหัสผ่าน sha1 เดิมที่มีอยู่ในคอลัมน์นี้จะยัง
-- ใช้งานได้ตามปกติ (PasswordHash::verify() รองรับ backward-compatibility)
-- และจะถูกอัปเกรดเป็นรูปแบบใหม่อัตโนมัติทันทีที่ผู้ใช้คนนั้น login สำเร็จครั้งถัดไป
-- ไม่ต้อง reset รหัสผ่านผู้ใช้คนใดเลย
-- =====================================================================

ALTER TABLE users MODIFY password VARCHAR(255) NOT NULL;
